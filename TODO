* test that a out-of-the-box yesod app works under mod_wai
* find what is the actual expected lifetime of a content_type,
  or convert that singly linked list to use apr_table
* don't always return OK
* request_rec::err_headers_out?
* error document when catch toplevel exception?
* what other response headers need to go into special variables?
* r->header_only? (HEAD)?
* figure out isSecure