* find what is the actual expected lifetime of a content_type,
  or convert that singly linked list to use apr_table
* dont always return OK? better than raising error
* redirects, and request_rec::err_headers_out?
* figure out isSecure

this version
* separate out Apache glue and Wai glue, have datatype:
    data HaskellApp = WaiApp Wai.Application | HackApp Hack.Whatever | RawApp ApacheApp
* make -> cabal
